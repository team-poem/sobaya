---
description: Phase 0 — split the feature, enumerate test cases, probe each red, write plan.md
---
Run the `tdd` skill's Phase 0 for the feature described in `spec.md`$ARGUMENTS.
Probe every candidate with `../../tdd-set/bin/probe.sh <package-dir> -` and record only RED
results in `plan.md` as checkbox + code block. Show the list with each probe result and stop
for approval. Write no implementation code.
